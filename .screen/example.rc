####################################
# AUTOGENERATED FROM: example.conf #
####################################

source ${HOME}/.screenrc

setenv CURRENT_SCREEN_ROOT ${HOME}/some/path

chdir ${CURRENT_SCREEN_ROOT}bare
screen -t bare
exec ls --color

chdir ${CURRENT_SCREEN_ROOT}/deeply/nested/structure
screen -t structure
exec ls --color

chdir ${CURRENT_SCREEN_ROOT}/title/specified
screen -t title
exec ls --color

select 0

chdir ${CURRENT_SCREEN_ROOT}
unsetenv CURRENT_SCREEN_ROOT
